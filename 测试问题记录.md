# 智能租房系统 — 测试问题记录

> 本文档记录开发与联调测试过程中发现的问题、原因及解决方案，供答辩与后续维护参考。
> 记录时间：2026-09-17

## 一、后端环境与启动类问题

### 1. 应用启动失败：Port 55938 was already in use
- **现象**：`java -jar` 启动后反复报 `Web server failed to start. Port 55938 was already in use`，应用无法启动。
- **原因**：运行环境会把 `server.port` 篡改为 55938（与 IDE 自身 IPC 端口冲突）。`application.yml` 里明明写的是 8081，但被运行时覆盖了。
- **解决**：运行 jar 时显式加 JVM 参数 `-Dserver.port=8081` 强制覆盖（JVM 系统属性优先级最高）。用户在 IDEA 直接读 `application.yml` 的 8081 不受影响。

### 2. 停掉服务后端口仍被占用
- **现象**：用 `kill` 杀掉启动的进程后，8081 端口仍被占用，下次启动失败。
- **原因**：Windows 的 `java.exe` 会把真正的 JVM 派生到子进程，只杀父进程杀不掉子进程。
- **解决**：用 `taskkill /F /T /PID <父pid>` 杀整个进程树。

### 3. 错误信息统一变成“系统开小差了”
- **现象**：注册重复用户名、密码过短等业务错误，前端拿到的都是 “系统开小差了，请稍后再试”。
- **原因**：`UserService` 里用 `RuntimeException` 抛业务异常，被全局异常处理器的兜底分支 `handleOther(Exception)` 捕获，返回通用提示。
- **解决**：业务异常统一改用 `BusinessException`，由 `GlobalExceptionHandler` 的 `handleBusiness` 分支返回具体提示信息。

### 4. 内置管理员无法登录
- **现象**：`admin/123456` 一直登录失败。
- **原因**：`init.sql` 里管理员密码写的是占位符 hash `$2a$10$PLACEHOLDER`，不是 `123456` 的 BCrypt 密文。
- **解决**：删除 SQL 里的占位插入，改为 `AdminInitRunner`（CommandLineRunner）在首次启动时用 BCrypt 生成 `admin/123456` 并播种。

---

## 二、功能逻辑类问题

### 5. 中文关键词搜索匹配不到结果
- **现象**：自然语言筛选接口 `GET /house/search?q=近地铁` 对中文关键词返回空结果。
- **原因**：测试脚本直接把中文拼进 URL，未做 URL 编码，服务端收到乱码，LIKE 匹配为空。
- **解决**：前端/调用方对 query 参数做 `encodeURIComponent`（Python 用 `urllib.parse.quote`）。

### 6. 房源浏览接口返回 401
- **现象**：测试时访问房源列表/详情报 401 “未登录”。
- **原因**：系统的拦截器对未加白名单的接口统一要求登录；房源列表/详情属于登录后浏览，测试脚本没带 token。
- **解决**：明确“浏览需登录”的设计，测试与前端统一带 token 访问。

### 7. AI 问答：单主题问题却拼接出多条答案
- **现象**：问“押金怎么退”，回答里拼接了押金、合同、宠物三条知识。
- **原因**：知识检索的关键词加权打分太松，只要知识条目“答案里含关键词”就给 1 分（弱匹配），导致多个条目都被纳入。
- **解决**：给检索加相关度阈值（`score >= 3`，即关键词必须命中条目的“问题”或“分类”才视为相关），并补全关键词表（如“租房”）。

### 8. RAG 组合答案分隔符与内容冲突
- **现象**：判断“是否拼接了多条知识”时，单条知识里也出现了分隔符，误判。
- **原因**：组合多条知识用 `；` 作分隔，而知识库某条答案文本内部本来就含 `；`。
- **解决**：组合分隔符改用换行符 `\n`，与答案内容天然区分，前端 `white-space:pre-line` 展示。

### 9. 语义检索误匹配地名
- **现象**：搜“附近有湖”，却匹配到了地址为“海淀区”的房源。
- **原因**：概念词典里收录了单字词“海”，而“海淀区”包含“海”字，被当成了“水景”语义。
- **解决**：概念词典移除容易撞地名的单字词（海/江/河/猫/狗），只保留关键单字“湖”和多字词（池塘/湖泊/地铁/钓鱼等）。

---

## 三、前后端联调类问题

### 10. 前端 import 位置错误
- **现象**：`Home.vue` 里 `reactive` 的 `import` 写在了使用语句之后，导致编译/运行报错。
- **解决**：所有 `import` 统一放在 `<script setup>` 顶部。

### 11. 前后端分离的跨域问题
- **现象**：前端（5173）直接调后端（8081）会跨域。
- **解决**：主方案用 Vite 开发代理（`/api` 前缀转发到 8081），浏览器始终同源；同时后端加 CORS 兜底配置（`WebConfig.addCorsMappings`），即使直连也不跨域。

### 12. 前端鉴权头格式
- **现象/要点**：后端拦截器直接取 `Authorization` 头作为 JWT（不加 `Bearer` 前缀），前端若加了 `Bearer ` 前缀会导致校验失败。
- **解决**：前端 axios 请求拦截器里直接把 token 原样放入 `Authorization` 头。

---

## 四、环境/运维类问题

### 13. git push 时好时坏
- **现象**：`git push` 经常报 `Failed to connect to github.com:443`。
- **原因**：运行环境对 github.com 的网络访问不稳定（npm 源正常，github 不通）。
- **解决**：本地提交完整保留历史；网络恢复后重试 `git push`，或在本地执行推送。
